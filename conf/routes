# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                Application.index
GET     /index                           Application.index
POST    /createPost                      Application.createPost
GET		/getPost						 Application.getPost
GET		/getComments						 Application.getComments
POST	/createComment					 Application.createComment
GET		/searchPosts					 Application.searchPosts
GET		/getSimilarWords				 Application.getSimilarWords
# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public

# Catch all
*       /{controller}/{action}                  {controller}.{action}
